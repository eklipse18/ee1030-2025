#include <stdio.h>
// #include "lib/png/readpng.h"
// #include "lib/svd.h"
// #include "lib/compress.c"


int main(int argc, const char * argv[]) {
    // int ihdr[7];
    // int k;
    // fscanf(argv[2], "%d", &k);
    // int ** array = readpng(argv[1], ihdr);
    // int *** svd_result = svd(array, ihdr[0], ihdr[1]);
    // int ** U = svd_result[0];
    // int ** S = svd_result[1];
    // int ** V = svd_result[2];
    // int ** U = compress(U, 0, k);
    // int ** S = compress(S, 1, k);
    // int ** V = compress(V, 2, k);
    // int *** recomposed = malloc(3 * sizeof(int**));
    // recomposed[0] = U;
    // recomposed[1] = S;
    // recomposed[2] = V;
    // int ** new_arr = svd_recompose(recomposed, ihdr[0], ihdr[1]);
    // savepng("out.png", new_arr, ihdr);
    return 0;
}